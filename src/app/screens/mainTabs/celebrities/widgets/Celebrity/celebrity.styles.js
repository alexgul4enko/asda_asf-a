import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  btn: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    height: 400,
  },
})
