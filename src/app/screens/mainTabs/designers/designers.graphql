query Designers($first: Int, $cursor: String, $search: String) {
  designers (first: $first, after: $cursor, filter: { search: $search }) {
    totalCount,
    pageInfo {
      endCursor,
      hasNextPage
    },
    edges {
      node {
        id,
        firstName,
        lastName,
        avatar {
          url
        }
      }
    }
  }
}
