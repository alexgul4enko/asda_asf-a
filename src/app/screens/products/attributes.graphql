query ATTRIBUTES ($slug: String)  {
  attributes(first: 100, filter: { inCategorySlug: $slug }) {
    edges {
      node {
        id
        name
        slug
        displayType
        values {
          id
          name
          slug
          image {
            url
          }
        }
      }
    }
  }
}