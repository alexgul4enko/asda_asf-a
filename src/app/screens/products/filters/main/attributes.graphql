query ATTRIBUTES ($filter: AttributeFilterInput)  {
  attributes(first: 100, filter: $filter) {
    edges {
      node {
        id
        name
        slug
        displayType
        values {
          id
          name
          slug
          image {
            url
          }
        }
      }
    }
  }
}